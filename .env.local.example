# Server-side only. Browser talks to /api/* which Next.js rewrites to API_URL.
API_URL=http://localhost:8000
# Must equal SECRET_KEY in backend/.env (used by middleware.ts to verify the session cookie)
JWT_SECRET=
